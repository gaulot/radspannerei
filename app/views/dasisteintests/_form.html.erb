<%= form_for(@dasisteintest) do |f| %>
  <% if @dasisteintest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dasisteintest.errors.count, "error") %> prohibited this dasisteintest from being saved:</h2>

      <ul>
      <% @dasisteintest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
